import { EliminomailPipe } from './eliminomail.pipe';

describe('EliminomailPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminomailPipe();
    expect(pipe).toBeTruthy();
  });
});
