import { BotonesSombraRedDirective } from './botones-sombra-red.directive';

describe('BotonesSombraRedDirective', () => {
  it('should create an instance', () => {
    const directive = new BotonesSombraRedDirective();
    expect(directive).toBeTruthy();
  });
});
