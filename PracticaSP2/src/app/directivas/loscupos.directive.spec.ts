import { LoscuposDirective } from './loscupos.directive';

describe('LoscuposDirective', () => {
  it('should create an instance', () => {
    const directive = new LoscuposDirective();
    expect(directive).toBeTruthy();
  });
});
